pipeline {
    agent any

    stages {

        stage('GitHub Connected') {
            steps {
                echo 'GitHub repository connected successfully!'
            }
        }

        stage('Check Files') {
            steps {
                sh 'ls -la'
            }
        }
    }
}
